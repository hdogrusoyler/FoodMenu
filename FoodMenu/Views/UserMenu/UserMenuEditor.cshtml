@model Menu
@{
    ViewData["Title"] = "Edit";
}
<div class="m-4">
    <div class="bg-success text-white p-4">
        <div class="col">@Model.MenuDate tarihli Menü</div>
    </div>

    <form asp-action="EditMenu" method="post">
        <input type="hidden" asp-for="MenuId" />

        <div class="row">
            <div class="col-8 m-3">
                <br />
                @foreach (User p in ViewBag.Users)
                {
                    <div class="form-row">
                        <div class="form-group col-1">
                            @if (p.UserMenus.Any(ps => ps.MenuId == Model.MenuId))
                            {
                                <input type="checkbox" name="userid" value="@p.UserId" checked />
                            }
                            else
                            {
                                <input type="checkbox" name="userid" value="@p.UserId" />
                            }
                        </div>
                        <div class="form-group col">
                            <label class="form-control-label">@p.Name @p.Surname</label>
                        </div>

                    </div>
                }
            </div>
        </div>

        <button type="submit" class="btn btn-primary">Güncelle</button>

    </form>

</div>

